import 'package:flutter/material.dart';

class VaultTemplate extends StatelessWidget {
  const VaultTemplate({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}